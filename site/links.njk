---
layout: default
title: Recommended Reading (and Listening/Watching)
description: Scott Watermasysk's Links
pagination:
  data: collections.links
  size: 25
  alias: links
---
<h1>Recommended Links</h1>
<p>Here are some external posts, videos, podcasts and more that I found interesting.</p>

{% for link in links %}
<h2 class="link"><a href="{{link.data.source_url}}">{{link.data.title}}</a></h2>
{{ link.templateContent | safe }}
{% endfor %}

{% if pagination.previousPageHref or pagination.nextPageHref %}
  <div class="flex mt-10  mx-auto">
    {% if pagination.nextPageHref %}
    <a class="flex-auto text-left" href="{{pagination.nextPageHref | ts}}">← Older Posts</a>
    {% endif %}
    {% if pagination.previousPageHref %}
    <a class="flex-auto text-right" href="{{pagination.previousPageHref | ts}}">Newer Posts →</a>
    {% endif %}
  </div>
{% endif %}
